<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>KSS Quantum Shield - 5 Layers</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.1.1/crypto-js.min.js"></script>
    <style>
        :root {
            --neon-blue: #00f3ff;
            --neon-pink: #ff00ff;
            --bg-color: #050505;
        }

        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

        body {
            background-color: var(--bg-color);
            color: #fff;
            font-family: 'Segoe UI', sans-serif;
            margin: 0;
            padding: 30px 3px 20px 3px; /* Quy tắc 3 cạnh */
            display: flex;
            justify-content: center;
            min-height: 100vh;
        }

        .container {
            width: 100%;
            max-width: 450px;
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(0, 243, 255, 0.2);
            border-radius: 20px;
            padding: 20px;
        }

        h2 {
            text-align: center;
            font-size: 1.2rem;
            color: var(--neon-blue);
            text-shadow: 0 0 10px var(--neon-blue);
            margin-bottom: 20px;
        }

        .box {
            background: rgba(0, 0, 0, 0.5);
            border-radius: 12px;
            padding: 12px;
            margin-bottom: 12px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        label { display: block; font-size: 0.7rem; color: #666; margin-bottom: 6px; text-transform: uppercase; }

        input, select, textarea {
            width: 100%;
            background: #000;
            border: 1px solid #222;
            border-radius: 8px;
            padding: 12px;
            color: #fff;
            font-size: 16px;
        }

        /* Tầng bảo mật 5: Màu Hex */
        .color-wrapper {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        input[type="color"] {
            width: 50px;
            height: 50px;
            padding: 0;
            border: 2px solid #222;
            cursor: pointer;
            background: none;
        }

        .shape-grid {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 8px;
            margin-top: 5px;
        }

        .shape-item {
            aspect-ratio: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #0a0a0a;
            border: 1px solid #222;
            border-radius: 8px;
            cursor: pointer;
        }

        .shape-item svg { width: 22px; height: 22px; stroke: #444; stroke-width: 2; fill: none; }
        .shape-item.active { border-color: var(--neon-pink); background: rgba(255, 0, 255, 0.05); }
        .shape-item.active svg { stroke: var(--neon-pink); }

        .btn-group { display: flex; flex-direction: column; gap: 10px; margin-top: 10px; }
        button { padding: 16px; border: none; border-radius: 10px; font-weight: bold; text-transform: uppercase; cursor: pointer; }
        .btn-enc { background: var(--neon-blue); color: #000; }
        .btn-dec { background: transparent; border: 1px solid var(--neon-pink); color: var(--neon-pink); }

        textarea { margin-top: 15px; height: 100px; color: #00f3ff; font-size: 13px; font-family: monospace; }
    </style>
</head>
<body>

<div class="container">
    <h2>KSS 5-LAYER SHIELD</h2>

    <div class="box">
        <label>01. File nguồn</label>
        <input type="file" id="f">
    </div>

    <div class="box">
        <label>02. Câu hỏi gợi ý & Trả lời</label>
        <select id="q" style="margin-bottom: 8px;">
            <option>Chọn câu hỏi gợi ý mật mã...</option>
            <option>Mã hex này đại diện cho điều gì?</option>
            <option>Màu sắc của kỷ niệm?</option>
        </select>
        <input type="text" id="a" placeholder="Câu trả lời...">
    </div>

    <div class="box">
        <div style="display:flex; gap:10px">
            <div style="flex:2">
                <label>03. 12 Số</label>
                <input type="number" id="n" placeholder="000000000000" oninput="if(value.length>12)value=value.slice(0,12)">
            </div>
            <div style="flex:1">
                <label>04. 3 Ký tự </label>
                <input type="text" id="s" maxlength="3" placeholder="!@#">
            </div>
        </div>
    </div>

    <div class="box">
        <label>05. Chọn 1 hình vẽ </label>
        <div class="shape-grid" id="g"></div>
    </div>

    <div class="box">
        <label>06. Chọn mã màu</label>
        <div class="color-wrapper">
            <input type="color" id="c" value="#00f3ff">
            <span id="hexVal" style="font-family: monospace; color: var(--neon-blue);">#00f3ff</span>
        </div>
    </div>

    <div class="btn-group">
        <button class="btn-enc" onclick="run('enc')">Mã hoá & Tải (KSS V2)</button>
        <button class="btn-dec" onclick="run('dec')">Khôi phục file gốc</button>
    </div>

    <textarea id="o" readonly placeholder="Kết quả giải mã..."></textarea>
</div>

<script>
    let sel = null;
    const icons = [
        '<circle cx="12" cy="12" r="9"/>','<rect x="4" y="4" width="16" height="16"/>',
        '<path d="M12 2 L22 20 L2 20 Z"/>','<path d="M12 2 L15 9 L22 9 L17 14 L19 21 L12 17 L5 21 L7 14 L2 9 L9 9 Z"/>',
        '<ellipse cx="12" cy="12" rx="9" ry="5"/>','<path d="M3 12 h18 M12 3 v18"/>',
        '<path d="M5 5 L19 19 M19 5 L5 19"/>','<circle cx="12" cy="12" r="4"/>',
        '<path d="M12 21 a9 9 0 1 0 0 -18 v18"/>','<rect x="8" y="2" width="8" height="20" rx="2"/>'
    ];

    const grid = document.getElementById('g');
    icons.forEach((svg, i) => {
        const d = document.createElement('div');
        d.className = 'shape-item';
        d.innerHTML = `<svg viewBox="0 0 24 24">${svg}</svg>`;
        d.onclick = () => { sel = i; document.querySelectorAll('.shape-item').forEach(e => e.classList.remove('active')); d.classList.add('active'); };
        grid.appendChild(d);
    });

    document.getElementById('c').oninput = function() {
        document.getElementById('hexVal').innerText = this.value;
        document.getElementById('hexVal').style.color = this.value;
    };

    async function run(m) {
        const file = document.getElementById('f').files[0];
        const ans = document.getElementById('a').value;
        const num = document.getElementById('n').value;
        const spec = document.getElementById('s').value;
        const hex = document.getElementById('c').value;

        if (!file || !ans || num.length < 12 || sel === null) {
            alert("Vui lòng hoàn thành 5 tầng bảo mật!"); return;
        }

        // TẠO KHÓA TỪ 5 TẦNG: Trả lời + 12 Số + 3 Ký tự + Hình + Mã màu Hex
        const raw = ans + num + spec + sel + hex;
        const salt = CryptoJS.SHA512(hex + num).toString(); // Muối kết hợp Màu và Số

        const r = new FileReader();
        r.onload = function(e) {
            try {
                // Tiêu chuẩn KSS V2: PBKDF2 + 100,000 vòng + SHA512
                const k = CryptoJS.PBKDF2(raw, salt, { keySize: 512/32, iterations: 100000, hasher: CryptoJS.algo.SHA512 }).toString();
                const content = e.target.result;

                if (m === 'enc') {
                    const encrypted = CryptoJS.AES.encrypt(content, k).toString();
                    const blob = new Blob(["KSS_V2_5L|" + encrypted], {type: 'text/plain'});
                    const a = document.createElement('a');
                    a.href = URL.createObjectURL(blob);
                    a.download = `V2_SECURE_${Math.floor(Math.random()*9999)}.txt`;
                    a.click();
                    location.reload(); // Tự hủy dữ liệu form
                } else {
                    let data = content;
                    if(content.startsWith("KSS_V2_5L|")) data = content.split("|")[1];
                    const dec = CryptoJS.AES.decrypt(data, k);
                    const orig = dec.toString(CryptoJS.enc.Utf8);
                    if (!orig) throw new Error();
                    document.getElementById('o').value = orig;
                }
            } catch (err) { alert("Sai thông tin bảo mật! Truy cập bị từ chối."); }
        };
        r.readAsText(file);
    }
</script>
</body>
</html>
